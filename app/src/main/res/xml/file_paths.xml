<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage path, replace with your specific requirements -->
    <external-path
        name="external_files"
        path="." /> <!-- Allows access to the root of external storage -->

    <!-- Cache directory for your app -->
    <cache-path
        name="cache_files"
        path="." /> <!-- Allows access to the cache directory -->
</paths>
